/vendor/
/.env
/storage/logs/*.log
/storage/cache/*
/storage/reports/*
/storage/tmp/*
/storage/framework/sessions/*
/database/backups/*.sql
/database/backups/*.gz
/database/backups/*.enc
/public/uploads/students/*
/public/uploads/teachers/*
!.gitkeep
.DS_Store
*.swp
node_modules/

# Terminal credentials. The sketch is tracked and update.bat replaces every
# tracked file, so the API key and Wi-Fi password live beside it in a file
# that is not — see firmware/L_SIAMS_Terminal/secrets.h.example.
/firmware/*/secrets.h
